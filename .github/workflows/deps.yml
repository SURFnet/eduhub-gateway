name: Check deps

on:
  schedule:
    - cron: '0 0 * * 1'
  push:
  pull_request:

jobs:
  deps:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/setup-node@v6
      with:
        node-version: 22

    - uses: actions/checkout@v6
    - run: npm ci
    - run: npm audit --omit dev
