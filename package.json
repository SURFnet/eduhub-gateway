{
  "name": "ooapi",
  "description": "Express Gateway Instance Bootstraped from Command Line",
  "repository": {},
  "license": "UNLICENSED",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "standard --env mocha && mocha --require test/integration.hook.js -t 60000 test/*.test.js test/**/*.test.js"
  },
  "devDependencies": {
    "mocha": "^8.1.3",
    "testcontainers": "^3.4.11",
    "standard": "*"
  },
  "dependencies": {
    "express-gateway": "^1.16.10",
    "express-openapi-validator": "^4.0.0-beta.2"
  }
}
